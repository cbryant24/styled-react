import { css } from 'styled-components';
import { get, includes, omit, range } from 'lodash';

const red = '#e42d42'
const blue = '#2d9ce4'
const white = '#ffffff'
const black = '#000000'

export const colors = {
  red,
  blue,
  white,
  black
}

const theme = {
  colors
}

export default theme;